services:
  - type: web
    name: carcheck-pro
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: CLAUDE_API_KEY
        sync: false
